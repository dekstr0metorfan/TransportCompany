/**
 * This class initializes a graphical user interface for the simulation
 * The GUI is made visible to the user.
 */
public class Simulation {
    /**
     * Constructs a new Simulation object.
     * Initializes a graphical user interface (GUI) and sets it to be visible.
     */
    public Simulation(){
        GUI gui = new GUI();
        gui.setVisible(true);
    }
}
